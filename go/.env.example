# ============================================================================
# twinstar-bosskills — Go server configuration
#
# Copy to .env. Makefile auto-imports values; cmd/server reads via os.Getenv.
# Override any value by prefixing the make invocation:
#   BK_HTTP_ADDR=:8080 make server
# ============================================================================

# ----------------------------------------------------------------------------
# ClickHouse — native protocol (preferred for clickhouse-go v2).
# ----------------------------------------------------------------------------
BK_CH_DSN=clickhouse://default:@127.0.0.1:9000/bosskills?dial_timeout=10s&max_execution_time=60

# ----------------------------------------------------------------------------
# Upstream Twinstar API. Used by sync + item lookups + icon proxy.
# ----------------------------------------------------------------------------
BK_TWINSTAR_API_URL=https://twinstar-api.twinstar-wow.com

# ----------------------------------------------------------------------------
# HMAC secrets. Must match the existing SvelteKit deployment so cookies
# issued by the old app keep working after cutover.
# ----------------------------------------------------------------------------
SECRET_TOKEN_GUILD=
SECRET_TOKEN_ADMIN=

# ----------------------------------------------------------------------------
# On-disk cache directories. Persist across restarts.
# ----------------------------------------------------------------------------
BK_ICON_DIR=./var/icons
BK_ITEM_DIR=./var/items

# ----------------------------------------------------------------------------
# HTTP server.
# ----------------------------------------------------------------------------
BK_HTTP_ADDR=:3000
